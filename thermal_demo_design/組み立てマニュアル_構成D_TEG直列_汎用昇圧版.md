# 組み立てマニュアル — 構成D改: TEG直列＋汎用昇圧モジュール版

> **このマニュアルの目的:**
> 初期不良だった CJMCU-3108（LTC3108搭載）モジュールの代わりに、
> **ペルチェ素子（TEG）6枚の直列接続** ＋ **汎用 0.9V DC-DC昇圧モジュール** を使って
> Maxwell's Demon Energy Harvester（構成D）を動作させる手順書です。

> [!IMPORTANT]
> **ファームウェア（プログラム）の変更は一切不要です。**
> 既存の `maxwell_demon_harvester_esp32c3.ino` がそのまま動作します。

---

## 目次

1. [仕組みの違い（なぜこの方法で代用できるのか）](#1-仕組みの違い)
2. [部品リスト](#2-部品リスト)
3. [TEG 6枚の直列接続](#3-teg-6枚の直列接続)
4. [USB昇圧モジュールの準備](#4-usb昇圧モジュールの準備)
5. [ブレッドボード上の全体配線](#5-ブレッドボード上の全体配線)
6. [配線チェックリスト](#6-配線チェックリスト)
7. [動作テスト](#7-動作テスト)
8. [トラブルシューティング](#8-トラブルシューティング)

---

## 1. 仕組みの違い

### 元の構成（CJMCU-3108 / LTC3108）

```text
TEG 1枚 (20〜50mV) ──→ [LTC3108 昇圧IC] ──→ 3.3V出力 ──→ スーパーキャパシタ蓄電
                        (20mVから昇圧可能な
                         特殊な超低電圧昇圧IC)
```

### 今回の代替構成（TEG直列 ＋ USB昇圧モジュール）

```text
TEG 6枚直列 (0.9V〜3V) ──→ [USB DC-DC昇圧] ──→ 5V固定出力 ──→ スーパーキャパシタ蓄電
                             (0.9V以上で起動する
                              USB給電用の昇圧モジュール)
```

#### なぜ 6枚直列にするのか？

ペルチェ素子（TEG）1枚が生み出す電圧は、温度差10℃のとき約 **150mV〜200mV** です。
汎用昇圧モジュールは **0.9V 以上** ないと起動しません。

| TEG枚数 | 温度差10℃での発電電圧 | 温度差20℃での発電電圧 | 汎用昇圧モジュール |
| :---: | :---: | :---: | :---: |
| 1枚 | 約 0.15V〜0.2V | 約 0.3V〜0.4V | ❌ 起動不可 |
| 2枚 | 約 0.3V〜0.4V | 約 0.6V〜0.8V | ❌ ギリギリ不足 |
| 4枚 | 約 0.6V〜0.8V | 約 1.2V〜1.6V | ⚠️ 温度差大なら可 |
| **6枚** | **約 0.9V〜1.2V** | **約 1.8V〜2.4V** | **✅ 安定起動！** |

6枚直列にすることで、温度差10℃でも **0.9V〜1.2V** の電圧が得られ、汎用昇圧モジュールが安定して起動します！

---

## 2. 部品リスト

### 変更・追加部品

| # | 部品名 | 仕様 | 数量 | 備考 |
| :---: | :--- | :--- | :---: | :--- |
| ★1 | ペルチェ素子 (TEG) | SP1848-27145 (40×40mm) | **6** | 直列接続して電圧を6倍にする |
| ★2 | USB DC-DC 昇圧モジュール | 0.9V〜5V 入力 → USB 5V固定出力 | **1** | CJMCU-3108 の代わり |
| ★3-a | 不要な USBケーブル | USB-A オス端子があるもの | **1** | 昇圧モジュールの出力取り出し用（切断して使用） |
| ★3 | アルミヒートシンク | 40×40mm 程度 | **5** | 冷却面用（保冷剤でも代用可） |

### そのまま使う部品（元の構成Dと同じ）

| # | 部品名 | 仕様 | 数量 |
| :---: | :--- | :--- | :---: |
| 1 | マイコンボード | XIAO ESP32-C3 | 1 |
| 2 | スーパーキャパシタ | 1.0F 5.5V | 1 |
| 3 | ショットキーダイオード | 1N5819 / BAT54 | **2** |
| 4 | 電解コンデンサ | 100μF 16V | 1 |
| 5 | MOSFET | IRLZ44N (N-ch) | 1 |
| 6 | 温度センサー | DS18B20 × 2本 | 2 |
| 7 | 抵抗 330Ω | 1/4W | 1 |
| 8 | 抵抗 100Ω | 1/4W | 1 |
| 9 | 抵抗 220Ω | 1/4W | 1 |
| 10 | 抵抗 10kΩ | 1/4W | 4 |
| 11 | 抵抗 4.7kΩ | 1/4W | 1 |
| 12 | 赤色LED | 5mm | 1 |
| 13 | 緑色LED | 5mm 高輝度 | 1 |
| 14 | ジャンパワイヤ (オス-オス) | 各色 | 多数 |

---

## 3. TEG 6枚の直列接続

### 3-1. 直列接続とは？

乾電池を並べて繋ぐのと同じ原理です。
1個1.5Vの乾電池を2本直列にすると3Vになるように、
TEGも直列に繋ぐと **電圧が足し算** されます。

```text
     ┌──── TEG1 ────┐   ┌──── TEG2 ────┐         ┌──── TEG6 ────┐
     │  赤(+) 黒(-) │   │  赤(+) 黒(-) │  ・・・  │  赤(+) 黒(-) │
     └───┬──────┬───┘   └───┬──────┬───┘         └───┬──────┬───┘
         │      └───────────┘      └── ・・・ ──────┘      │
         │    （黒と赤を繋ぐ）    （黒と赤を繋ぐ）          │
         │                                                  │
    ここが全体の           ここが全体の
     プラス(+)              マイナス(-)
    昇圧モジュール          昇圧モジュール
     VIN+ へ                 GND へ
```

### 3-2. 具体的な繋ぎ方（6枚の場合）

> [!CAUTION]
> **すべてのTEGの「温める面」と「冷やす面」の向きを統一してください！**
> 1枚でも上下が逆になると、その1枚の電圧が差し引かれてしまい、合計電圧が下がります。

#### 手順

1. **TEG 6枚を横一列に並べます。**
   すべてのTEGの印字面（文字がある面）を上向きに統一します。

2. **隣同士の TEG を「黒線（ー）→ 赤線（＋）」の順で繋いでいきます。**

```text
  TEG1        TEG2        TEG3        TEG4        TEG5        TEG6
  赤  黒 ──── 赤  黒 ──── 赤  黒 ──── 赤  黒 ──── 赤  黒 ──── 赤  黒
  │                                                                │
  │                                                                │
  ▼                                                                ▼
 全体の (+)                                                  全体の (-)
 昇圧モジュール VIN+ へ                               昇圧モジュール GND へ
```

3. **繋ぎ方の詳細（5箇所の接続）:**

| 接続番号 | 繋ぐ線 | 繋ぐ相手 | 方法 |
| :---: | :--- | :--- | :--- |
| ① | TEG1 の **黒線(-)** | TEG2 の **赤線(+)** | ハンダ付けまたはワニ口クリップ |
| ② | TEG2 の **黒線(-)** | TEG3 の **赤線(+)** | 同上 |
| ③ | TEG3 の **黒線(-)** | TEG4 の **赤線(+)** | 同上 |
| ④ | TEG4 の **黒線(-)** | TEG5 の **赤線(+)** | 同上 |
| ⑤ | TEG5 の **黒線(-)** | TEG6 の **赤線(+)** | 同上 |

4. **最終的に残るのは2本の線だけです：**
   * **TEG1 の赤線(+)** → 昇圧モジュールの **VIN+** へ
   * **TEG6 の黒線(-)** → 昇圧モジュールの **GND** へ

> [!TIP]
> **接続のコツ:**
> - ハンダ付けが一番確実ですが、ワニ口クリップやねじり接続でもOKです。
> - 接続部分にはビニールテープなどを巻いて、金属がむき出しにならないようにしましょう（ショート防止）。
> - ブレッドボードに直接挿せない場合は、ジャンパワイヤの先端にハンダ付けしてください。

### 3-3. ヒートシンク（放熱板）の取り付け

温度差を効率よく作るため、TEGの **冷やす面（下側）** にヒートシンクを取り付けます。

```text
   ☀️ 温かいもの（お湯マグカップ・手のひら等）
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ┃  TEG1  ┃  TEG2  ┃  TEG3  ┃  TEG4  ┃  TEG5  ┃  TEG6  ┃  ← 印字面（上）
   ┗━━━━━━━━┻━━━━━━━━┻━━━━━━━━┻━━━━━━━━┻━━━━━━━━┻━━━━━━━━┛
   ┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
   │ ヒート ││ ヒート ││ ヒート ││ ヒート ││ ヒート │  ← ヒートシンク5枚
   │ シンク ││ シンク ││ シンク ││ シンク ││ シンク │    (6枚目は無くてもOK)
   └────────┘└────────┘└────────┘└────────┘└────────┘
   ❄️ 冷たいもの（保冷剤・アイスパック・机の上等）
```

> [!TIP]
> **ヒートシンクが5枚しかない場合:**
> 6枚目のTEGはヒートシンクなしで机の表面に直置きでも問題ありません。
> テーブルの表面自体が放熱板の代わりになります。
>
> **熱伝導グリス（シリコングリス）** があれば、TEGとヒートシンクの間に薄く塗ると密着度が上がり、発電量がアップします。

---

## 4. USB昇圧モジュールの準備

### 4-1. モジュールの確認

お手元の「DC-DC USB 0.9V-5V に 5V 電圧変換ステップアップブースターモジュール」は、以下のような形をしています：

```text
  ┌──────────────────────────────────────┐
  │  DC-DC USB 0.9V-5V → 5V 昇圧モジュール  │
  │                                      │
  │  ┌──────────────┐                    │
  │  │  USB-A メス  │ ← 出力（5V固定）    │
  │  │  端子（穴）  │   ここにUSBケーブルを挿す
  │  └──────────────┘                    │
  │                                      │
  │         基板の裏側・端に               │
  │        入力パッド（2つ）               │
  │        [VIN+]  [VIN-]                │
  └──────────────────────────────────────┘
```

**このモジュールの特徴:**

| 項目 | 内容 |
| :--- | :--- |
| 入力 | 基板上の **2つのパッド**（VIN+ と VIN-）にハンダ付けで接続 |
| 出力 | **USB-A メス端子**（穴の形状）から 5V 固定出力 |
| 出力電圧 | **5V 固定**（調整ネジはありません） |

> [!NOTE]
> **5V 固定出力で問題ありません！** 理由：
> - スーパーキャパシタの定格は 5.5V なので、5V 充電は安全です。
> - ダイオード D2 を通過すると約 0.3V 下がり、VCC_node には約 4.7V が届きます。これは USB 接続時と**同じ電圧**です。
> - VSTORE の分圧（10kΩ/10kΩ）で GPIO4 に届く電圧は 5V ÷ 2 = 2.5V で、ADC の上限（3.3V）以内です。

### 4-2. USBケーブルの加工（出力の取り出し）

このモジュールの出力は USB-A メス端子なので、ブレッドボードに直接挿すことができません。
**不要な USB ケーブルを1本加工して、出力を取り出します。**

#### 用意するもの
- 不要な USB ケーブル 1本（USB-A オス端子があるもの。充電専用ケーブルでOK）
- ハサミまたはニッパー
- ワイヤーストリッパー（なければカッターナイフで慎重に被覆をむく）

#### 手順

1. **USB ケーブルの「USB-A オス端子」ではない方の端を、ハサミで切断します。**

```text
  [USB-A オス端子] ─────────── ✂️ ここで切る ─── [もう片方の端子]
       │                                           (不要)
   この端子を昇圧モジュールに挿す
```

2. **切断した側の被覆を 3cm ほどむきます。**
   中から **4本の細い線** が出てきます。

```text
   外側の被覆をむくと...

   🔴 赤色の線  = +5V（電源プラス）  ← ★これを使う
   ⚫ 黒色の線  = GND（電源マイナス） ← ★これを使う
   🟢 緑色の線  = データ線            ← 使わない（テープで絶縁）
   ⚪ 白色の線  = データ線            ← 使わない（テープで絶縁）
```

3. **赤色と黒色の線だけを残し、各線の先端を 1cm ほどむきます。**
   緑色と白色の線はビニールテープで先端を巻いて絶縁してください。

4. **むき出しになった赤色・黒色の線の先端に、ジャンパワイヤ（オス）をハンダ付けします。**
   （※ハンダ付けが難しい場合は、ねじり接続＋ビニールテープでもOKです）

> [!TIP]
> **ジャンパワイヤをハンダ付けしておくと、ブレッドボードに直接挿せるようになります！**

5. **完成した出力ケーブルの接続先：**

| ケーブルの線 | 接続先 |
| :--- | :--- |
| **赤色の線（+5V）** | ブレッドボードの **VSTORE 列**（スーパーキャパシタの＋側・D2アノード側） |
| **黒色の線（GND）** | ブレッドボードの **上部青ライン（GND）** |

### 4-3. 入力側の接続

昇圧モジュール基板の入力パッドに、TEG直列の出力線を接続します。

| 入力パッド | 接続するもの |
| :--- | :--- |
| **VIN+**（プラス側パッド） | TEG1 の **赤線(+)**（全体のプラス） |
| **VIN-**（マイナス側パッド） | TEG6 の **黒線(-)**（全体のマイナス） |

> [!NOTE]
> 入力パッドへの接続はハンダ付けが最も確実です。
> TEGの線を直接パッドにハンダ付けするか、ジャンパワイヤを経由してください。

### 4-4. 出力電圧の事前確認

ブレッドボードに繋ぐ**前に**、昇圧モジュール単体で出力を確認します。

1. 入力側に TEG直列（温度差あり）または乾電池 1本（1.5V）を接続します。
2. USB-A メス端子に加工済みの USB ケーブルを挿します。
3. テスター（`DCV 20`）で赤色の線と黒色の線の間の電圧を測ります。
4. **`4.8V` ～ `5.2V`** が表示されれば正常です！ ✅

---

## 5. ブレッドボード上の配線手順（Step by Step）

> [!IMPORTANT]
> **CJMCU-3108（LTC3108）モジュールはブレッドボードに挿しません。**
> 代わりに、USB昇圧モジュールから伸ばしたUSBケーブルの **赤線（+5V）** と **黒線（GND）** の2本だけをブレッドボードに挿します。

以下、Step 1 から順番に配線していきます。

---

### Step 1: マイコン（XIAO ESP32-C3）をブレッドボードに挿す

ブレッドボードの中央の溝（みぞ）をまたぐようにマイコンを挿します。
USB-C端子が外側を向くように置きます。

```text
   +-------------------+
   |  [+] (上部赤ライン) |  ← VCC_node（3.3V系の電源ライン）
   |  [-] (上部青ライン) |  ← GND（グランド）
   +-------------------+
     a b c d e f g h i
   +-------------------+
  1| O O O O O | O O O | ← 左1: GPIO2    右1: 5V
  2| O O O O O | O O O | ← 左2: GPIO3    右2: GND
  3| O O O O O | O O O | ← 左3: GPIO4    右3: 3V3
  4| O O O O O | O O O | ← 左4: GPIO5    右4: D7
    ...
```

---

### Step 2: 電源レールの接続（ジャンパワイヤ 2本）

マイコンの電源ピンを、ブレッドボードの電源ラインに繋ぎます。

| ジャンパワイヤ | 接続元 | 接続先 |
| :--- | :--- | :--- |
| ① | XIAO **右側2番目 (GND)** の列 | **上部青ライン** |
| ② | XIAO **右側3番目 (3V3)** の列 | **上部赤ライン** |

---

### Step 3: ダイオード D1（USB電源保護用）

USB給電時の電源を VCC_node（上部赤ライン）に供給するためのダイオードです。

| 部品 | 接続元（挿す場所） | 接続先（もう片方） |
| :--- | :--- | :--- |
| D1 (1N5819) の **帯なし側（アノード）** | XIAO **右側1番目 (5V)** の列 | — |
| D1 (1N5819) の **銀帯あり側（カソード）** | — | **上部赤ライン (VCC_node)** |

```text
  XIAO 5V ──→|── (D1: 銀帯→) ──→ 上部赤ライン(VCC_node)
           帯なし側          銀帯あり側
```

---

### Step 4: 平滑コンデンサ（100μF 電解コンデンサ）

電源の安定化のために VCC_node と GND の間に入れます。

| 部品の足 | 挿す場所 |
| :--- | :--- |
| **長い足 (+)** | **上部赤ライン (VCC_node)** |
| **短い足 (-) / 白帯あり** | **上部青ライン (GND)** |

> [!CAUTION]
> 電解コンデンサには極性があります。＋ーを逆に挿さないでください。

---

### Step 5: USB昇圧モジュールの出力（USBケーブルの赤線・黒線）をブレッドボードに接続

ここが今回の構成で**最も重要な接続**です。
セクション4で加工したUSBケーブルの赤線と黒線を、ブレッドボードに挿します。

> [!IMPORTANT]
> **ブレッドボード上で「VSTORE列」を1つ決めてください。**
> 空いている列（例: 25列目など）を選び、以下の部品をすべてこの列に挿します。
> ブレッドボードの同じ列（縦5穴）は内部で繋がっているので、同じ列に挿すだけで電気的に接続されます。

**「VSTORE列」に挿すもの（すべて同じ列の穴に挿す）:**

| 穴 | 挿すもの |
| :---: | :--- |
| 穴1 | USB昇圧ケーブルの **赤線（+5V）** |
| 穴2 | スーパーキャパシタの **長い足（＋）** |
| 穴3 | 10kΩ抵抗の片足（VSTORE分圧用 上段 → もう片方はGPIO4の列へ） |
| 穴4 | 100Ω抵抗の片足（緑LED用 → もう片方は緑LEDのアノードへ） |
| 穴5 | D2 (1N5819) の **帯なし側（アノード）** |

**GNDに挿すもの:**

| 挿すもの | 挿す場所 |
| :--- | :--- |
| USB昇圧ケーブルの **黒線（GND）** | **上部青ライン (GND)** |
| スーパーキャパシタの **短い足（ー）/ 帯あり** | **上部青ライン (GND)** |

```text
  ★ VSTORE列 (例: 25列目) に5つの部品が集まるイメージ:

  USB赤線(+5V)─┐
  キャパシタ(+)─┤
  10kΩ上段────┤── 同じ列（内部で全部つながる）
  100Ω────────┤
  D2帯なし側──┘

  USB黒線(GND) ────→ 上部青ライン (GND)
  キャパシタ(-) ───→ 上部青ライン (GND)
```

---

### Step 6: ダイオード D2（TEG電源→VCC_node供給用）

D2 の片足は Step 5 で VSTORE列に挿しました（帯なし側＝アノード）。
もう片方の足（銀帯あり側＝カソード）を VCC_node に繋ぎます。

| 部品の足 | 挿す場所 |
| :--- | :--- |
| D2 の **帯なし側（アノード）** | VSTORE列（Step 5 で挿し済み） |
| D2 の **銀帯あり側（カソード）** | **上部赤ライン (VCC_node)** |

```text
  VSTORE列 ──→|── (D2: 銀帯→) ──→ 上部赤ライン(VCC_node)
            帯なし側           銀帯あり側
```

---

### Step 7: VSTORE 電圧モニタ用の分圧回路（10kΩ × 2）

マイコンが蓄電電圧（VSTORE）をADCで読み取るための分圧抵抗です。

| 抵抗 | 片足の挿し先 | もう片足の挿し先 |
| :--- | :--- | :--- |
| 10kΩ 上段 | **VSTORE列**（Step 5 で挿し済み） | XIAO **左3番目 (GPIO4)** の列 |
| 10kΩ 下段 | XIAO **左3番目 (GPIO4)** の列 | **上部青ライン (GND)** |

```text
  VSTORE列 ──[10kΩ上段]──→ GPIO4の列 ──[10kΩ下段]──→ GND
                              ↑
                        ここをマイコンが
                        ADCで電圧計測する
```

---

### Step 8: MOSFET（IRLZ44N）スイッチ回路

緑LEDの点灯を制御するスイッチです。文字が書いてある面を手前にして挿します。

| MOSFETのピン | 位置 | 接続先 |
| :--- | :--- | :--- |
| **Pin 1 (Gate / 左)** | 220Ω抵抗経由で | XIAO **左2番目 (GPIO3)** の列 |
| **Pin 1 (Gate / 左)** | 10kΩ抵抗経由で | **上部青ライン (GND)** （プルダウン） |
| **Pin 2 (Drain / 中央)** | | 緑LEDの短い足（カソード）へ |
| **Pin 3 (Source / 右)** | | **上部青ライン (GND)** |

```text
  GPIO3 ──[220Ω]──┬──→ Gate (左ピン)
                   │
                [10kΩ]
                   │
                  GND
```

---

### Step 9: 緑色LED（悪魔LED）回路

蓄電したエネルギーで光るLEDです。

| 部品 | 片足の挿し先 | もう片足の挿し先 |
| :--- | :--- | :--- |
| 100Ω 抵抗 | **VSTORE列**（Step 5 で挿し済み） | 緑LEDの**長い足（アノード＋）** の列 |
| 緑色LED **長い足 (+)** | 100Ω抵抗の先 | — |
| 緑色LED **短い足 (-)** | — | MOSFET **Pin 2 (Drain / 中央)** の列 |

```text
  VSTORE列 ──[100Ω]──→ 緑LED(+長足) → 緑LED(-短足) ──→ MOSFET Drain
```

---

### Step 10: 赤色LED（パッシブLED）回路

マイコンが直接制御するLEDです。

| 部品 | 片足の挿し先 | もう片足の挿し先 |
| :--- | :--- | :--- |
| 赤色LED **長い足 (+)** | XIAO **左4番目 (GPIO5)** の列 | — |
| 赤色LED **短い足 (-)** | — | 330Ω抵抗へ |
| 330Ω 抵抗 | 赤LEDの短い足の列 | **上部青ライン (GND)** |
| 10kΩ プルダウン抵抗 | XIAO **左4番目 (GPIO5)** の列 | **上部青ライン (GND)** |

---

### Step 11: 温度センサー（DS18B20 × 2本）

| 線の色 | 2本とも挿す場所 |
| :--- | :--- |
| **赤線 (VCC)** × 2本 | **上部赤ライン (VCC_node)** |
| **黒線 (GND)** × 2本 | **上部青ライン (GND)** |
| **黄線 (Data)** × 2本 | XIAO **左1番目 (GPIO2)** の列 |

さらに、プルアップ抵抗を追加します：

| 部品 | 片足の挿し先 | もう片足の挿し先 |
| :--- | :--- | :--- |
| 4.7kΩ 抵抗 | XIAO **左1番目 (GPIO2)** の列 | **上部赤ライン (VCC_node)** |

---

### 全体の電気の流れ（まとめ図）

```text
  ☀️ お湯（温め）              ❄️ 保冷剤（冷やし）
      ↓                            ↓
  ┌─────────────────────────────────────┐
  │   TEG 6枚 直列接続                   │
  │   TEG1赤(+) ─ TEG2 ─ ... ─ TEG6黒(-)│
  └──┬──────────────────────────────┬────┘
     │ 全体の＋                    │ 全体のー
     ▼                            ▼
  [USB昇圧モジュール VIN+]   [USB昇圧モジュール VIN-]
     │                            │
     │  (モジュール内部で 0.9V→5V に昇圧)
     │                            │
     ▼                            ▼
  [USBケーブル 赤線 +5V]     [USBケーブル 黒線 GND]
     │                            │
     │  ブレッドボードへ            │  ブレッドボードへ
     ▼                            ▼
  ┌─ VSTORE列 ─────────┐      上部青ライン (GND)
  │                     │
  ├→ スーパーキャパシタ(+)  ←── 蓄電する「電気のプール」
  │
  ├→ D2 (1N5819)        ──→ 上部赤ライン (VCC_node)
  │   帯なし → 銀帯           ├→ XIAO 3V3
  │                           ├→ DS18B20 電源
  │                           └→ 4.7kΩプルアップ
  │
  ├→ 10kΩ上段 → GPIO4 → 10kΩ下段 → GND
  │              (マイコンが電圧を読み取る)
  │
  └→ 100Ω → 緑LED(+) → 緑LED(-) → MOSFET Drain
                                      │
                               Gate ← GPIO3 (220Ω経由)
                               Source → GND
  ```

---

## 6. 配線チェックリスト

以下の全項目に ✓ を入れてから、動作テストに進んでください。
ファームウェアのピン定義と完全に一致しています。

### 🔌 電源系統（マイコン ↔ ブレッドボード電源レール）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 1 | マイコン GND → 上部 GND バス | XIAO 右側2番目 (GND) | 上部青ライン | `[ ]` |
| 2 | マイコン 3V3 → 上部 VCC_node | XIAO 右側3番目 (3V3) | 上部赤ライン | `[ ]` |

### ⚡ ダイオードOR回路（USB ＆ TEG ハイブリッド給電）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 3 | D1 アノード(帯なし) → XIAO 5V | D1 (1N5819) 帯なし側 | XIAO 右側1番目 (5V) | `[ ]` |
| 4 | D1 カソード(銀帯) → VCC_node | D1 銀帯あり側 | 上部赤ライン | `[ ]` |
| 5 | D2 アノード(帯なし) → **USBケーブル赤線(+5V)の列** | D2 (1N5819) 帯なし側 | USBケーブル赤線と同じ列 | `[ ]` |
| 6 | D2 カソード(銀帯) → VCC_node | D2 銀帯あり側 | 上部赤ライン | `[ ]` |
| 7 | 100μF コンデンサ (+) → VCC_node | 長足 (+) | 上部赤ライン | `[ ]` |
| 8 | 100μF コンデンサ (-) → GND | 短足 (-/白帯) | 上部青ライン | `[ ]` |

### 🔋 発電・昇圧・蓄電ユニット（TEG×6直列 → 汎用昇圧 → スーパーキャパシタ）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 9 | TEG1 赤線(+)（全体の＋） → 昇圧 VIN+ パッド | TEG1 赤線 | USB昇圧モジュール VIN+ パッド | `[ ]` |
| 10 | TEG6 黒線(-)（全体のー） → 昇圧 VIN- パッド | TEG6 黒線 | USB昇圧モジュール VIN- パッド | `[ ]` |
| 11 | TEG間の直列接続 5箇所 | 前のTEGの黒線(-) | 次のTEGの赤線(+) | `[ ]` |
| 12 | USBケーブル **赤線(+5V)** → スーパーキャパシタ (+) | USBケーブル赤線 | 1.0F キャパシタ 長足(+) と同じ列 | `[ ]` |
| 13 | スーパーキャパシタ (-) → GND | 1.0F キャパシタ 短足(-) | 上部青ライン (GND) | `[ ]` |
| 14 | USBケーブル **黒線(GND)** → GND | USBケーブル黒線 | 上部青ライン (GND) | `[ ]` |

### 🟢 MOSFET ＆ 緑色LED（悪魔の制御回路）— GPIO3

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 15 | **左2 (GPIO3)** → 220Ω → MOSFET Gate | XIAO 左側2番目 (A1/D1) | 220Ω → IRLZ44N Pin 1 (Gate) | `[ ]` |
| 16 | MOSFET Gate → **10kΩ** → GND (プルダウン) | IRLZ44N Pin 1 (Gate) | 上部青ライン (GND) | `[ ]` |
| 17 | MOSFET Source → GND | IRLZ44N Pin 3 (Source/右) | 上部青ライン (GND) | `[ ]` |
| 18 | VSTORE → 100Ω → 緑LED(+) → 緑LED(-) → Drain | スーパーキャパシタ(+) | 100Ω → 緑LED → IRLZ44N Pin 2 (Drain) | `[ ]` |

### 🔴 赤色LED（パッシブ LED）— GPIO5（左4番目）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 19 | **左4 (GPIO5)** → 赤LED(+) → 赤LED(-) → 330Ω → GND | XIAO 左側4番目 (A3/D3) | 赤LED → 330Ω → 上部青ライン (GND) | `[ ]` |
| 20 | **左4 (GPIO5)** → **10kΩ** → GND (プルダウン) | XIAO 左側4番目 (A3/D3) | 上部青ライン (GND) | `[ ]` |

### 📊 電圧モニタ（分圧回路）— GPIO4（左3番目）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 21 | VSTORE → 10kΩ → **左3 (GPIO4)** | スーパーキャパシタ(+) | 10kΩ → XIAO 左側3番目 (A2/D2) | `[ ]` |
| 22 | **左3 (GPIO4)** → 10kΩ → GND | XIAO 左側3番目 (A2/D2) | 10kΩ → 上部青ライン (GND) | `[ ]` |

### 🌡️ 温度センサー（DS18B20 × 2本）— GPIO2（左1番目）

| # | チェック項目 | 接続元 | 接続先 | 確認 |
| :---: | :--- | :--- | :--- | :---: |
| 23 | DS18B20 赤線×2 → VCC_node | 赤線 (2本) | 上部赤ライン (3V3) | `[ ]` |
| 24 | DS18B20 黒線×2 → GND | 黒線 (2本) | 上部青ライン (GND) | `[ ]` |
| 25 | DS18B20 黄線×2 → **左1 (GPIO2)** | 黄/青線 (2本) | XIAO 左側1番目 (A0/D0) | `[ ]` |
| 26 | 4.7kΩプルアップ: **左1 (GPIO2)** ↔ VCC_node | XIAO 左側1番目 (A0/D0) | 上部赤ライン (3V3) | `[ ]` |

### 📌 ピン配置サマリー（最終確認用）

```text
  ファームウェア定義             物理位置        役割
  ─────────────────────────────────────────────────────
  ONE_WIRE_BUS   = GPIO2     → 左1番目      → DS18B20 データ
  MOSFET_GATE_PIN= GPIO3     → 左2番目      → MOSFET Gate (220Ω経由)
  VSTORE_PIN     = GPIO4     → 左3番目      → VSTORE 分圧 ADC
  LED_PASSIVE_PIN= GPIO5     → 左4番目      → 赤色LED 制御
```

---

## 7. 動作テスト

### テスト1: USB昇圧モジュール単体テスト（TEG＋昇圧のみ）

ブレッドボードに繋ぐ前に、昇圧モジュールが正しく動くか単体で確認します。

1. TEG6枚直列に温度差を与えます（上にお湯、下に保冷剤）。
2. USB-A メス端子に加工済みの USB ケーブルを挿します。
3. テスター（`DCV 20`）でUSBケーブルの **赤線** と **黒線** の間の電圧を測定します。
4. **`4.8V` ～ `5.2V`** が安定して出ていれば成功です！ ✅

### テスト2: スーパーキャパシタの充電テスト

1. USBケーブルの赤線（+5V）をスーパーキャパシタの長足(+)へ、黒線(GND)を短足(-)へ繋ぎます。
2. 温度差を与えると、テスター（`DCV 20`）でスーパーキャパシタの電圧が **じわじわと上昇** していきます。
3. **`2.5V` 以上** まで上がれば、蓄電成功です！ ✅

### テスト3: 全体テスト（USB接続してシリアルモニタ確認）

1. チェックリストの全項目を確認済みの状態で、USBケーブル（パソコン接続用）をマイコンに接続します。
2. Arduino IDE のシリアルモニター（115200 baud）を開きます。
3. TEGに温度差を与えます。
4. シリアルモニタで以下を確認します：
   - `V_store_mV` が **上昇** していく → ✅ 蓄電成功！
   - `deltaT_C` が **10℃以上** → ✅ 十分な温度差！
   - `flash_count` が **1以上** になる → ✅ 緑LEDが点灯（悪魔が仕事をしている）！

---

## 8. トラブルシューティング

| 症状 | 考えられる原因 | 対処法 |
| :--- | :--- | :--- |
| USBケーブルの赤線・黒線から電圧が出ない | TEGの温度差が不足（入力電圧が0.9V未満） | お湯の温度を上げる / 保冷剤を追加する / TEGの枚数を増やす |
| USBケーブルの赤線・黒線から電圧が出ない | TEGの直列接続で1枚の極性が逆 | 全TEGの赤線(+)と黒線(-)の向きを再確認 |
| USBケーブルの赤線・黒線から電圧が出ない | USBケーブルの赤線・黒線が断線している | 別のUSBケーブルで試す / テスターの導通モードで赤線・黒線の断線を確認 |
| `V_store_mV` が 220mV 前後で変わらない | 分圧抵抗（10kΩ×2）がフローティング | 上段10kΩがスーパーキャパシタ(+)に、下段10kΩがGNDに繋がっているか確認 |
| `V_store_mV` が上がるが緑LEDが光らない | MOSFET の Gate配線の問題 | GPIO3 → 220Ω → Gate、Gate → 10kΩ → GND を確認 |
| DS18B20 が `-127.0℃` を返す | OneWire 配線不良 | 4.7kΩプルアップ抵抗が GPIO2 と VCC_node 間に接続されているか確認 |
| テスターで TEG直列の電圧を測ると「マイナス」が出る | 温める面と冷やす面が逆 | お湯と保冷剤の位置を上下逆にする |

---

> [!TIP]
> **テスター（DT-830D）による事前チェックが最も確実です！**
> 1. TEG直列の出力電圧（`DCV 2000m` レンジ）→ 温度差10℃で **0.9V (900mV)以上** あるか？
> 2. USBケーブルの赤線-黒線間の出力電圧（`DCV 20` レンジ）→ **4.8V ～ 5.2V** が出ているか？
> 3. スーパーキャパシタの電圧（`DCV 20` レンジ）→ 時間とともに **上昇** しているか？
>
> この3つが全部OKなら、回路は100%正常に動作します！

---

**作成日:** 2026-09-15
**対象構成:** Configuration D 改（TEG直列＋汎用昇圧モジュール版）
**ベースファームウェア:** `maxwell_demon_harvester_esp32c3.ino`（変更不要）
